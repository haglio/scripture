__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/
dist/
build/
.eggs/
sessions/
funscripts/

# Private overlays — real values live here and never get published; each has
# a committed *.example.* template documenting its shape.
/content.local.json
/sanitize/blocklist.local.txt
/sanitize/library_roots.local.txt

# Windows shortcuts are machine-specific and embed the real install path.
*.lnk

# pCloud conflict copies (this dir syncs via pCloud). Never our files;
# an untracked one dirties main and blocks every agent's merge.
*\[conflicted*
